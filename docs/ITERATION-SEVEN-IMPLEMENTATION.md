# V1.0 迭代七实现记录

> 日期：2026-08-17
> 状态：功能闭环已实现，待专项验收

## 实现范围

- 正式教学大纲派生结课问卷草稿，包含课程目标自评、教学内容、教学方法、考核方式、学习支持、实践能力和开放题。
- 教师逐题审核、时间设置、实名/匿名模式、乐观并发保存、发布冻结、关闭和聚合结果界面。
- 学生问卷列表、五级量表与开放题作答、必答校验、提交二次确认、一次性幂等提交和历史完成状态。
- 发布复用课程班级成员与站内通知；问卷不进入作业计分、成绩台账、知识证据或推荐闭环。
- 匿名参与与回答物理隔离：`CourseSurveyParticipation` 不保存回答外键，匿名 `CourseSurveyResponse.studentId` 为 `NULL`，匿名提交不写入可关联回答的实名审计。
- 小于 5 份回答时只保留人数和响应率；达到阈值后确定性计算题目、维度和课程目标均值/分布，并保存输入指纹与不可变汇总修订。
- 开放题清理邮箱、手机号、学号等显式个人标识，只输出共性主题计数和聚合文字，不向教师返回单份匿名回答。
- 教学质量报告自动冻结同班级最近一次已关闭问卷的聚合快照；学生自评与客观成绩/课程目标定量达成度分开表述。
- 报告文字通过统一 AI Service Layer 生成，只发送去标识化聚合；Provider 缺失、关闭、超时或输出无效时使用确定性降级。
- AI 结果只作为审核初稿：教师可逐项修改成绩分析、课程目标总体/分项目标分析、学生评价、课程总结和持续改进措施；确认后保存审核人、时间、备注和最终文字快照。
- AI 审核稿与正式 DOCX 分开私有存储；未完成人工确认时禁止下载正式 DOCX。确认后的正式版本不可原地修改，后续变化生成新报告版本。
- 生成器在运行时校验 2024 模板 SHA-256，以模板副本原位填充封面、统计表和图表；课程评价小组、学院意见、签字和日期保持模板空白。

## 数据与迁移

- 问卷 migration 为 `20260816100000_add_course_surveys`；报告人工审核新增 `20260817110000_add_quality_report_review`。仓库当前连同并行完成的班级解散和课程软删除迁移共 53 个 migration。
- 新增问卷、问题、参与、回答、答案和汇总修订模型，以及审计和通知枚举。
- migration 为追加式，不回填、不删除、不改写历史课程、成绩、作业、画像、推荐或报告数据。
- 报告审核迁移新增审核状态、最终文字、审核人和正式 DOCX 元数据；旧版报告保留为审核稿，需要按 v2 规则重新生成后再人工确认，不静默补写结论。
- 回滚策略：在尚无正式问卷数据时可回滚问卷 migration 新增表和枚举值；已有数据后应先导出问卷与汇总快照，不执行破坏性自动回滚。

## 权限与隐私

- 所有教师入口同时校验角色、课程归属、班级归属和问卷状态；跨教师资源返回统一不存在语义。
- 所有学生入口校验角色、有效班级成员关系、开放/截止时间和参与状态。
- 匿名回答没有教师可用的身份关联；报告只消费 `CourseSurveySummaryRevision`，不读取单份回答。
- 外部 AI 输入不含姓名、学号、邮箱、手机号或单份开放题，只包含课程聚合统计和已脱敏主题。

## 实际检查结果

- `npx prisma validate`：通过。
- `npm run format:check`：通过。
- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npx tsx --test tests/quality-reports/*.test.ts`：12/12 通过，覆盖 AI 严格输出与降级、确定性统计、问卷隔离、迁移、人工审核门禁、模板校验、权限和工作簿。
- `npm test` 等价全量命令：在继承仓库本地命令路径的标准运行环境中 358/358 通过。
- `npm run test:http-integration`：本轮未运行，当前进程未提供独立 `TEST_DATABASE_URL`；全量测试中的 seed 集成已在独立测试 Schema 成功应用 53 个 migration 并完成两次幂等 seed。
- `npm run build`：Next.js 生产构建通过，新增师生页面和 API 均进入路由清单。
- `npx tsx scripts/generate-quality-report-fixture.ts`：生成 DOCX 与 XLSX 成功；正式 DOCX 由 Microsoft Word 打开并导出为 7 页 PDF，逐页渲染检查无裁切、重叠、破表、缺字或签字区误填。
- DOCX 包审计：模板页眉、页脚、样式、字体、主题、Custom XML 和原始图片保持逐字节一致；生成包只修改正文、关系、设置与文档属性并追加统计图。

## 尚待专项验收

- 使用登录态教师和至少 5 名学生完成匿名/实名全流程人工浏览器验收。
- 使用真实 AI Provider 复验结构化输出、超时、修复重试和降级元数据。
- 使用登录态完成“AI 审核稿 → 教师修改 → 二次确认 → 正式 DOCX 下载”的浏览器人工验收。
- 当前固定模板已有明确映射；只有引入不同复杂模板时才增加上传与人工字段映射。
