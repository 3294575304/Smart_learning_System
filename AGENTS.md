# AGENTS.md

> 项目：智学课堂——AI 驱动的个性化教学平台
>
> 当前阶段：MVP 已验收；V1.0 迭代一至六已通过专项验收；迭代七问卷与报告功能闭环已实现、待专项验收；迭代八可靠性治理与整合验收为后续计划
>
> 计划书基线：《项目计划书》3.0 版（2026-08-20）

## 1. 文档作用与优先级

本文件用于约束在本仓库中工作的编码 Agent。开始任何任务前，先阅读本文件、`README.md`、相关业务文档和待修改代码。

发生冲突时按以下优先级处理：

1. 用户当前任务中的明确要求。
2. 本文件中的工程与安全约束。
3. 最新版《项目计划书》及验收标准。
4. `README.md`、现有代码、Prisma Schema 和测试所体现的当前实现。

如果文档与代码状态不一致：

- 先通过代码、Schema、migration、测试和 Git 状态确认事实。
- 不得静默覆盖已实现能力。
- 在交付说明中指出差异，以及本次采用的处理方式。

## 2. 项目定位

智学课堂服务管理员、教师和学生三类用户，连接课程建设、班级管理、作业测评、成绩分析、知识图谱、学生画像、个性化练习与教学质量分析。

学生学习闭环：

> 教师组织课程与题目 → 学生学习和作答 → 系统记录学习证据 → 更新学情画像 → 推荐针对性练习 → 学生再次练习 → 持续更新知识掌握状态

教师教学改进闭环：

> 导入教学大纲与学生名单 → 建立课程知识结构 → 采集成绩和出勤数据 → 分析班级学情与课程目标达成情况 → 发布结课自评问卷 → 生成教学质量分析 → 教师在平台外审查

## 3. 当前项目基线

以下 MVP 能力已经完成并通过演示验收，后续开发必须复用并保证不回归：

- 管理员、教师、学生登录与角色隔离。
- 管理员用户治理、系统配置、公告与审计记录。
- 教师创建班级、管理成员、维护题库、发布作业。
- 学生查看作业、自动保存答案、提交作答。
- 客观题自动批改、简答题人工批改、成绩发布与查询。
- 学生与班级基础学情分析、AI 学习建议。
- 个性化推荐练习、推荐记录、错题本复习。
- 作业、提醒、批改、分析、推荐与公告的站内通知。
- Prisma 数据模型、Zod 校验、统一 AI 服务抽象、异常降级、测试与构建流程。

禁止把已完成模块重新当作初始化任务开发。新增能力应与原有账号、班级、题库、作业、提交、成绩、分析和通知模型衔接。

截至 2026-08-20，以下 V1.0 能力也已进入必须复用、不得回归的实现基线：

- 课程模板、教师课程、课程—班级关联、版本化课程文件和安全下载。
- 固定名单解析、字段映射、预览、待认领身份、班级预分配、唯一学号认领注册、自动入班、导入幂等与审计；新名单不创建默认密码账号。
- 文本型教学大纲 PDF 提取、AI 结构化解析、严格 Schema 校验、解析诊断、不可变审核修订、显式发布和历史正式版本。
- 基于正式大纲的 Python 课程知识图谱草稿、AI `RELATED` 增强与降级、不可变审核修订、显式发布、版本差异以及 PostgreSQL 节点/关系。
- 基于正式大纲的可审核考核方案、课程/班级成绩台账、固定成绩模板导入导出、特殊状态、成绩发布、签到及纠正和版本化课程目标达成度。
- 教师题目的课程级主/次知识点绑定、乐观并发控制、来源图谱版本记录和审计。
- 作业首次发布时冻结题目—Concept 绑定与图谱解析状态；后续题目绑定或图谱版本变化不得改写历史快照。
- 自动/人工批改生成追加式知识证据，重新批改与撤销生成新修订；课程知识掌握度保存输入指纹、规则版本和不可变修订，并提供师生归属校验后的查询 API。
- 通用后台任务、判题 Attempt/租约/心跳/重试/取消、统一学习事件和远程执行器契约。
- Python 编程题不可变配置、作业发布冻结、公开样例运行、隐藏用例异步判题、确定性评分、重判/撤销、系统错误重试和学生隐藏字段隔离。
- 不可变画像快照、事件水位、证据门槛/置信度、出勤/活跃度/反思/客观掌握度分维度保存和师生证据下钻。
- AI 题目批量候选、严格 ID 范围校验、失败降级、教师预览/修改/确认、来源版本记录和不覆盖人工绑定。
- 课程正式图谱与教学进度驱动的推荐、六项版本化确定性策略、自主练习、Python 推荐异步判题、追加式反馈闭环，以及独立可确认、修改和删除的 AI 自述证据。
- 正式大纲派生的结课问卷草稿、教师审核、实名/匿名发布、一次性提交、匿名关联隔离、不可变汇总、小样本保护和开放题脱敏主题。
- 固定 2024 模板教学质量报告、平台/上传双数据源、不可变快照、问卷聚合、统一 AI 文字与确定性降级、教师逐项审核、正式 DOCX/XLSX 私有下载和审计。

当前实现边界必须明确：

- V1.0 尚未整体完成或通过整体验收。
- 迭代五已于 2026-08-11 通过专项验收；独立 gVisor 远程执行器真实安全能力复测 8/8、公开运行/隐藏判题/确定性成绩/课程 Concept 证据纵向集成、空库 migration、全量回归、生产构建和浏览器人工核验均通过。无人登录开机自启按目标部署环境选配，不属于功能专项验收门禁。
- 迭代六已于 2026-08-13 正式封口；48 个 migration 的空库 HTTP 集成、14/14 PostgreSQL 集成、336/336 单测、三套构建和开发库迁移状态均通过复验。
- MVP 通用推荐继续使用旧的全局 `KnowledgePoint`/`StudentKnowledgeMastery` 链路并保留历史兼容；迭代六新增的课程推荐链路独立消费课程 `KnowledgeGraphConcept` 掌握度，不回填或改写旧推荐。
- 结课问卷和 DOCX 教学质量报告功能闭环已实现，但登录态浏览器全流程、真实 AI Provider、持久化长任务 worker 与 V1.0 整体界面/部署验收尚未完成。
- 2026-08-20 全项目复验：376 项自动化测试中 375 项通过、1 项因未配置固定外部 PDF 路径跳过；53 个 migration 的空库 HTTP 集成通过；PostgreSQL 推荐/通知集成 14/14 通过；格式、Lint、类型、Prisma 校验、生产构建、沙箱执行器和判题 worker 构建通过。
- 迭代二、三专项验收记录见 `docs/ITERATION-TWO-THREE-ACCEPTANCE.md`，迭代四记录见 `docs/ITERATION-FOUR-ACCEPTANCE.md`，迭代五最终记录见 `docs/ITERATION-FIVE-FINAL-ACCEPTANCE.md`，迭代六最终记录见 `docs/ITERATION-SIX-FINAL-ACCEPTANCE.md`；原大纲解析异常消息契约失败已修复，并已补充固定样本实践项目与图谱交互验收测试。最新全量检查结果以实际命令输出和验收记录为准。

## 4. 当前版本目标

V1.0 只聚焦“Python 程序设计”一门课程，先做深完整教学流程，再在 V2.0 抽象为多课程可复用模板。

V1.0 核心范围：

1. 课程与课程模板基础模型。
2. 教学大纲 PDF 上传、解析、审核和版本管理。
3. Python 课程知识图谱生成、编辑、发布和题目关联。
4. 固定学生名单模板导入、账号匹配或创建、自动入班。
5. 学生待认领身份、唯一学号认领注册与自动入班。
6. 成绩项目、成绩台账、课程目标达成度和课堂签到。
7. Python 编程题在线运行与测试用例驱动的基础自动判题。
8. 学生画像、知识掌握状态、班级学情分析和证据查看。
9. 学生自主选知识点练习、AI 对话式自我画像与个性化题库推荐。
10. 基于教学大纲的结课教学质量自评问卷。
11. 按固定 DOCX 模板生成教学质量分析报告。
12. 教材、课件、实践指导书等可选教学材料入口。

当前推进顺序以“迭代七专项验收 → 持久化任务与安全治理 → 三角色 E2E/CI → 界面整合 → 可观测性、数据留存与部署演练 → V1.0 总验收”为默认。任何局部专项通过不得被解释为 V1.0 整体完成。

V1.1 才实现：

- Python 编程题的原子功能点、AST、代码规范和 AI 语义等多维评价。
- 基于课程材料检索的 AI 辅助出题与题目质量审查。
- Bloom 层级控制、阶段性自适应练习和增强学习路径。

V2.0 才实现：

- 可配置课程模板 Schema。
- 多课程知识图谱隔离与复用。
- 可配置的大纲提取、成绩、画像、推荐、题型和报告规则。

近期不做：

- 视频直播课堂、家长端、付费、社交功能。
- 复杂排课、多校区或多租户商业化。
- 大规模互联网题库爬取。
- 无教师审核的正式考试自动出题。
- 多语言编程评测。
- 在线报告审批、电子签名或正式归档流程。

## 5. 建设原则

1. **先做深一门课，再做通多门课。** 不为假设中的多课程场景过早引入复杂抽象。
2. **结构化计算优先，AI 负责增强。** 分数、权重、正确率、达成度和权限由确定性程序处理。
3. **教师拥有最终决定权。** AI 解析、知识点关系、题目映射、画像解释和报告文字必须可审核、可修改、可追溯。
4. **先推荐审核题库，再生成新题。** V1.0 不用临时 AI 题目替代正式题库。
5. **数据可解释、可追踪、可回退。** 保存来源、置信度、规则版本、数据版本和生成依据。
6. **兼容现有架构。** 除非任务有明确收益和迁移方案，不大规模更换技术栈或重写已验收模块。
7. **小步交付。** 每次只修改任务相关内容，避免顺手重构无关代码。

## 6. 技术栈

继续使用仓库现有技术栈：

- Next.js App Router
- React
- TypeScript
- Tailwind CSS
- shadcn/ui
- Next.js API Routes / Server Actions
- Prisma ORM
- PostgreSQL
- Zod
- ESLint 与 Prettier
- 统一 AI Service Layer

V1.0 知识图谱的权威数据源优先使用 PostgreSQL 节点表和关系表。没有经过性能和业务验证，不引入 Neo4j。

课程材料语义检索可优先评估 PostgreSQL + pgvector；只有出现明确的规模、检索能力或运维需求时，才考虑独立向量数据库。

## 7. 角色与授权边界

### ADMIN

- 管理用户、教师和学生。
- 管理公共题库、平台配置、公告和审计。
- 查看平台级运行状态。

### TEACHER

- 只能管理自己有权访问的课程、班级、学生、题目、作业、成绩、问卷和报告。
- 上传并审核教学大纲解析结果。
- 管理知识图谱、成绩方案、签到、题库映射和教学质量报告。
- 只能发起学生密码重置，不能查看学生当前密码。

### STUDENT

- 只能访问自己加入的课程与自己的作答、成绩、画像、推荐、错题和问卷。
- 可完成作业、签到、编程题和自主练习。
- 可查看画像依据，确认、修改或删除自己的自我反思信息。

所有服务端入口必须同时检查：

1. 身份。
2. 角色。
3. 课程或资源归属。
4. 操作状态是否允许。

禁止仅依赖页面隐藏、中间件或客户端参数实现授权。跨教师资源应避免暴露资源是否存在。

## 8. 数据库与迁移规则

任何数据库修改必须：

1. 先检查现有 Prisma Schema 和关系，优先复用已有模型。
2. 修改 `schema.prisma`。
3. 创建可审查的 migration。
4. 说明已有数据的迁移、回填、默认值和回滚策略。
5. 更新必要的 seed、fixture 和测试数据。
6. 检查历史班级、作业、提交、成绩、画像和推荐是否受影响。
7. 运行 Prisma 校验及相关测试。

禁止：

- 直接修改生产数据库。
- 用 `db push` 代替应提交的正式 migration。
- 无迁移方案地删除或改变已有字段语义。
- 为了通过唯一邮箱约束，给无邮箱学生伪造可投递邮箱地址。
- 在任何表中保存明文密码。

核心业务模型应包含一致的 ID、`createdAt` 和 `updatedAt`；需要审计或历史解释的数据还应有版本、状态或事件记录。

新增概念模型前，先确认是否能复用：

- `User`
- `Class`
- `ClassMember`
- `Question`
- `Assignment`
- `Submission`
- `StudentAnswer`
- 现有分析、推荐和通知模型

建议的新增领域包括课程与文档、课程结构、学生导入、成绩与出勤、编程判题、画像事件、问卷和质量报告。名称可根据现有代码调整，但业务语义不得丢失。

## 9. API 与服务层规则

所有 API 或 Server Action 必须：

1. 验证身份、角色、资源归属和当前状态。
2. 使用共享 Zod Schema 校验输入。
3. 对重复请求提供幂等或明确冲突处理。
4. 不直接返回数据库、文件服务、代码沙箱或 AI 提供商的内部错误。
5. 使用仓库已有统一响应和错误约定，不另造第二套格式。
6. 对敏感导入、成绩修改、账号创建、文件下载和报告生成写审计日志。

业务逻辑放入 `services/` 或现有领域层，不把大量逻辑写在页面组件或路由处理函数中。

事务用于保证同一业务动作的一致性，例如：

- 名单导入与账号入班。
- 成绩修改与审计。
- 问卷发布与任务状态。
- 报告任务与数据快照。

长耗时任务不得阻塞普通请求。以下操作应使用现有任务机制或新增统一后台任务：

- 教学大纲解析。
- 知识图谱生成。
- 批量学生导入。
- 题目知识点批量匹配。
- 学生画像批量重算。
- Python 代码执行与批量判题。
- 问卷开放题主题汇总。
- 教学质量报告生成。

后台任务至少记录状态、进度、成功数、失败数、错误原因、发起人、开始和结束时间、重试信息，并保证重试不会重复写入。

## 10. 前端与交互规则

所有新增页面和异步操作必须具备：

- loading 状态。
- empty 状态。
- error 状态。
- retry 或恢复路径。
- 权限不足提示。
- 成功反馈。

表单优先复用项目现有表单方案，并使用共享 Zod Schema。危险或不可逆操作必须二次确认。

关键交互要求：

- 文档处理和后台任务显示进度，不让用户长时间面对空白页面。
- AI 提取采用“原文—结构化结果”对照审核。
- 知识图谱支持缩放、筛选、搜索和节点详情。
- 图表提供文字摘要，不只依赖颜色传达状态。
- 画像结论支持查看证据和更新时间。
- 推荐题显示“为什么推荐给我”。
- AI 对话生成的练习条件必须以结构化形式展示并让学生确认。
- 问卷明确标记“不计入成绩”。
- 报告提供生成进度、结果预览和 DOCX 下载，不增加审批与签字界面。

复杂组件拆到 `components/` 或仓库现有组件目录；页面组件只负责组合、状态和交互。

## 11. AI 集成规则

业务代码禁止直接调用具体模型 SDK。所有 AI 能力必须通过统一 AI Service Layer 和可替换的 Provider 接口。

AI 适合负责：

- 教学大纲理解与结构提取。
- 知识图谱草稿和题目知识点候选关联。
- 学情诊断的自然语言解释。
- 推荐理由。
- 自我反思对话摘要。
- 结课问卷草稿和开放题主题汇总。
- 教学质量报告的分析文字。
- V1.1 的辅助出题和题目审查。

确定性程序必须负责：

- 身份、角色与资源归属判断。
- 分数、权重、正确率和课程目标达成度计算。
- 签到状态与数据汇总。
- 题目候选过滤和硬性规则。
- Python 测试用例执行结果及基础分数。
- 文件权限和下载次数。

所有 AI 输出必须：

1. 使用明确的结构化 Schema。
2. 通过 Zod 校验。
3. 记录模型、提示词和规则版本。
4. 保存必要的来源引用、置信度和生成时间。
5. 按策略限制重试次数。
6. 失败后回退到规则计算、基础统计或人工处理。

AI 故障不得导致：

- 无法登录。
- 无法提交作业。
- 无法运行确定性成绩计算。
- 无法查看已发布成绩。
- 已有核心数据损坏。

发送给外部 AI 的学生数据必须去标识化，不发送姓名、学号、手机号、初始密码等无关个人信息。

## 12. V1.0 领域规则

### 12.1 教学大纲

- V1.0 必须支持文本型 PDF。
- 已提供的《Python 程序设计（2024 版）课程教学大纲》是固定验收样本。
- DOCX 可保留兼容入口，但不是 V1.0 必须验收格式。
- 扫描 PDF 无法可靠解析时，提示更换文件或手动补充；OCR 可后续增强。
- AI 结果先成为待审核草稿，教师确认后才能成为正式课程数据。
- 核心字段必须能追溯到原文页码、章节或表格位置。

固定样本至少应正确识别：

- 3 项课程目标。
- 11 章理论内容，共 20 学时。
- 8 个实验项目，共 12 学时。
- 总学时 32。
- 平时表现、课程作业、期中考试、课程实验、期末考试。
- 权重 10%、5%、5%、20%、60%。

### 12.2 学生名单导入

V1.0 固定模板为已提供的旧版 `.xls`，并兼容表头完全一致的 `.xlsx`。模板有 1 个工作表、10 列和 96 条验收数据。

10 列表头顺序固定：

1. 学年学期(文本)
2. 课程号(文本)
3. 学号(文本)
4. 姓名(文本)
5. 班级(文本)
6. 成绩标识(文本)
7. 期末成绩(100.0%)(文本)
8. 特殊原因(文本)
9. 等级成绩类型(文本)
10. 备注(文本)

导入规则：

- 学号按文本读取，禁止科学计数法、精度或前导零丢失。
- 学号是学生身份匹配的主要依据。
- 已存在学号只入班，不重置密码。
- 不存在学号则创建待认领学生身份和班级预分配关系，不生成默认密码。
- 姓名相同但学号不同视为不同学生。
- 学期或课程号不匹配时进入待确认列表。
- 重复、缺失或非法学号进入错误列表。
- 同一文件重复导入必须幂等。
- 执行前展示新增、匹配、已入班和错误数据预览。
- 非法数据不能造成不可解释的部分写入。

学生身份认领安全：

- 名单导入只创建待认领身份和班级预分配关系，不创建默认密码账号。
- 学生必须在注册页使用唯一学号和名单姓名认领身份。
- 注册角色固定为 `STUDENT`，客户端不能指定角色。
- 学号存在、姓名匹配、学号未认领和邮箱未注册必须同时成立。
- 用户创建、学号绑定、认领状态更新和课程入班必须处于同一事务。
- 重复或并发认领只能有一个成功，事务失败必须完整回滚。
- 既有一次性账号和首次改密能力仅用于兼容历史账号，不用于新名单导入。
- 平台不负责通过邮件、短信、微信或其他外部渠道发送注册信息。

### 12.3 知识图谱

- Python 默认知识框架只作为解析参考，正式结构以教师确认的大纲为准。
- 节点和关系必须保存来源、置信度、审核状态和版本。
- 草稿可编辑；发布版本才可用于题目关联、画像和推荐。
- 新版本发布前展示差异。
- 已关联历史学习数据的知识点不得硬删除，使用停用、合并或迁移。
- 一道题可关联多个知识点，并区分主要、次要知识点及考查权重。
- 未经教师确认的题目—知识点关联不得进入正式画像和推荐。

### 12.4 成绩、达成度与签到

- 成绩项目和权重由大纲解析生成草稿，经教师确认后生效。
- 所有成绩项目权重之和必须为 100%。
- 样本默认总评公式为 10% + 5% + 5% + 20% + 60%。
- 缺考、缓考、作弊、免修等特殊状态必须与普通零分分开。
- 已发布成绩修改必须记录修改人、时间和原因。
- 总评和课程目标达成度由程序计算，并保存规则版本。
- 出勤由平台课堂签到产生，教师可补录或纠正正常、迟到、早退、请假、缺勤状态。
- 未在有效时间签到的学生可自动标记缺勤，但教师纠正应保留原记录和原因。
- 出勤只影响学习投入与风险提示，不直接等同于知识掌握。

### 12.5 Python 编程题与判题

V1.0 必须支持：

- 教师配置题目、标准代码、公开样例、隐藏测试用例和分值规则。
- 学生在浏览器编辑代码并运行公开样例。
- 提交后异步执行隐藏测试用例。
- 按可复现的测试结果计算基础成绩。
- 保存代码、运行结果、耗时、内存、错误类型和知识点。
- 区分语法错误、运行错误、超时和内存超限。
- 不向学生泄露隐藏测试用例或系统内部信息。

代码执行必须在隔离沙箱中进行，默认禁用网络，并限制：

- CPU。
- 内存。
- 执行时间。
- 文件系统访问。
- 进程和输出大小。

判题服务失败不能阻塞普通题目提交和已有成绩查看。V1.0 不把 AST、代码风格或 AI 语义评分作为承诺范围。

### 12.6 学情画像

- 原始学习事件必须保留，当前知识状态应可重算。
- 每个知识点保存掌握度、证据数量、置信度和更新时间。
- 正式分析保存画像快照；推荐记录引用当时的快照。
- 近期证据权重可更高，但单次错误不能直接判定为未掌握。
- 数据不足时显示“证据不足”，不生成过度确定结论。
- 画像采用可变化状态，禁止“差生”“能力低”等永久化或污名化标签。
- 教师和学生可查看结论依据。
- 出勤、活跃度、自述与客观知识掌握证据必须分开保存。

AI 对话式自我画像：

- 学生自述作为单独的“自我反思证据”。
- 学生可确认、修改或删除自述信息。
- 自述不得直接覆盖程序计算的知识掌握度。
- 自述与客观数据冲突时并列展示，并建议通过诊断题验证。

### 12.7 个性化推荐

V1.0 从已审核题库推荐，不临时生成未经审核的正式题目。

候选过滤至少排除：

- 非当前课程题目。
- 超出教学进度的知识点。
- 未审核、停用或缺少答案解析的题目。
- 近期重复题目。
- 与学生当前难度差距过大的题目。

推荐排序应综合薄弱知识点、先修关系、难度、错误类型、题目新鲜度和教师优先级；权重可配置并保存规则版本。

每次推荐保存目标知识点、题目、分数、理由、画像版本、规则版本、完成结果和掌握度变化。

学生可以：

- 在已发布图谱中选择知识点、题型、题量和难度。
- 通过 AI 对话将自然语言目标转换为结构化练习条件。

自主练习仍需经过课程范围、教学进度、题目审核和权限过滤，不计入正式课程成绩。

### 12.8 结课问卷

- 问卷口径从大纲中的课程目标、能力描述、教学内容、考核方式和评分标准提取。
- 在现有任务发布流程中增加或复用“问卷”类型。
- 问卷不计入课程成绩。
- 教师可编辑草稿，设置班级、发布时间、截止时间和实名或匿名模式。
- 统计响应人数、响应率、量表结果、课程目标自评和开放题主题。
- 学生自评属于定性证据，不替代客观成绩或课程目标定量达成度。
- 匿名模式不得向教师暴露身份与单份回答的关联。
- 小样本时限制细分统计，避免重新识别学生。

### 12.9 教学质量报告

V1.0 以已提供的空白 DOCX 模板为版式基线，以实际成品为内容对照样本。

报告数据包括：

- 课程基本信息和课程目标。
- 成绩构成、成绩分布和课程目标达成度。
- 出勤、作业、实验、测验和考试数据。
- 结课问卷汇总。
- 学生评价概括。
- 课程总结和持续改进建议。

生成规则：

- 数字与图表由确定性程序生成。
- AI 只根据结构化统计生成分析文字。
- 复杂模板提供字段映射确认，不猜测填充位置。
- 保存模板、数据快照、提示词、模型和生成记录版本。
- 审核意见、签字和日期保持空白。
- 生成后校验 DOCX 可打开性、表格结构、图表和关键字段。
- AI 失败时仍应生成只包含确定性统计的基础报告。
- 平台只生成可下载的待审查 DOCX；不实现在线审批、签字或归档状态机。

### 12.10 可选教学材料

- 教材、课件、实践指导书和其他教学材料均为可选。
- 未上传材料不得阻塞课程创建、名单导入、作业发布、日常教学或报告生成。
- 只有教师授权的课程材料可用于本课程检索或 AI 处理。

## 13. 文件与隐私安全

- 学生名单、成绩、历史账号表和报告文件不得使用永久公开链接。
- 每次文件读取或下载必须验证课程归属和用户权限。
- 文件类型不能只信任扩展名；检查 MIME、大小和可解析性。
- 上传文件使用受控存储键，避免路径穿越和文件名覆盖。
- 文档解析仅接收任务需要的文件。
- 历史账号表不得进入普通文件列表或长期保存明文版本。
- 匿名问卷只向教师提供聚合结果和脱敏意见。
- 敏感导出应设置合适的缓存和响应头，并写入审计。

## 14. 测试与验收

每个功能至少覆盖：

- 正常流程。
- 未登录。
- 角色不符。
- 跨课程或跨教师资源访问。
- 参数错误。
- 数据不存在。
- 重复请求或重复提交。
- 后台任务失败与重试。
- AI、文件解析或外部依赖异常。
- 历史数据兼容和原 MVP 回归。

重点专项测试：

- 固定 10 列旧版 `.xls` 和 96 条样本数据。
- 学号文本精度、前导零、重复导入和事务一致性。
- 唯一学号认领、姓名匹配、重复与并发认领、事务回滚和自动入班。
- 大纲样本的 3 项课程目标、11 章、8 个实验和 5 项权重。
- 知识图谱发布、版本差异和历史关系迁移。
- 成绩公式、特殊状态、签到补录和达成度复算。
- Python 沙箱隔离、超时、内存、网络、文件、隐藏用例和重复判题一致性。
- 画像证据、数据不足、自述隔离和权限。
- 推荐范围、难度、重复过滤、理由与版本追溯。
- 匿名问卷、低响应率和开放题脱敏。
- DOCX 数值一致性、字段映射、版式、图表与可打开性。

提交前按任务范围运行最相关的检查，并尽可能完成：

```bash
npm run format:check
npm run lint
npm run typecheck
npm test
npm run test:http-integration
npm run build
npx prisma validate
npx prisma migrate status
```

HTTP 集成测试必须使用独立的 `TEST_DATABASE_URL`，不得连接开发库或生产库。无法运行的检查必须说明原因，不得声称通过。

## 15. 编码规范

- TypeScript 使用明确类型，禁止无理由使用 `any`、非空断言或关闭类型检查。
- 函数和服务保持单一职责。
- 使用有意义的领域命名。
- 复用共享 Schema、权限检查、错误类型和组件。
- 不复制大段业务逻辑。
- 注释解释“为什么”，不复述显而易见的代码。
- 不为满足测试而加入生产环境特例。
- 不吞掉异常；记录可诊断信息，但不得记录密码、令牌或敏感学生数据。
- 新配置必须提供安全默认值、环境变量说明和启动校验。

## 16. Git 与工作区规则

开始修改前：

1. 检查 `git status`。
2. 阅读相关文件和测试。
3. 识别用户已有的未提交修改。

修改过程中：

- 只改当前任务相关文件。
- 保留用户已有修改，不覆盖、不回滚。
- 不使用 `git add .`。
- 不提交 `.env`、密钥、`node_modules`、`.next`、构建产物或包含学生隐私的真实样本输出。
- 不直接操作 `main` 分支历史。
- 不执行破坏性 Git 命令。

完成后：

1. 检查 `git diff --check`。
2. 检查 `git diff` 和 `git status`，确认没有无关修改。
3. 运行与改动相关的测试。
4. 只有用户明确要求时才 commit 或 push。

提交信息使用 Conventional Commits，例如：

```text
feat: add syllabus parsing workflow
fix: preserve student number formatting on import
test: cover programming judge timeouts
docs: align agent guidance with v1 roadmap
```

## 17. 开发工作流

收到开发任务后：

1. 明确需求、范围和验收标准。
2. 检查现有实现、数据模型、测试和未提交修改。
3. 确定最小可行改动及迁移、权限、安全和回归影响。
4. 用户要求实现时直接完成安全且范围明确的工作；只有关键产品选择缺失、存在破坏性风险或需要扩大权限时再询问。
5. 实现数据层、服务层、接口和界面，避免只完成表面页面。
6. 补充正常、异常、权限和回归测试。
7. 运行质量检查并修复本次改动引入的问题。
8. 检查差异并交付结果。

每个迭代或较大功能必须能够说明：

- 需求与验收标准。
- 数据模型和迁移变化。
- API 和页面变化。
- 权限与隐私处理。
- 正常与异常流程测试。
- 数据迁移或回滚说明。
- 演示步骤。
- 已知限制。

## 18. 当前推荐实施顺序

除非用户明确调整优先级，V1.0 按以下状态和依赖推进：

1. **已完成并验收：**课程、课程模板、文件和批量导入基础。
2. **已完成并通过专项验收：**教学大纲结构化解析与审核。
3. **已完成并通过专项验收：**Python 知识图谱。
4. **已完成并通过专项验收：**成绩、课程目标达成度与出勤台账。
5. **已完成并通过专项验收：**题目知识点绑定、作业快照、统一学习事件、答题证据、版本化掌握度、Python 基础判题、画像快照、证据界面和 AI 批量题目候选。
6. **已完成并通过专项验收：**课程图谱驱动的个性化题库推荐、自主练习与 AI 对话画像。
7. **当前优先级、待专项验收：**结课教学质量自评问卷与 DOCX 教学质量分析报告登录态/真实 AI 验收。
8. **待实施：**持久化长任务 worker、安全响应头、限流与电子表格解析加固。
9. **待实施：**三角色浏览器 E2E、CI 与测试环境隔离。
10. **待实施：**界面流程整合、可观测性、数据留存、部署恢复演练与 V1.0 总验收。

涉及知识图谱、题目、作业和掌握度时，必须保持以下版本边界：

- `KnowledgeGraphConcept` 是跨正式图谱版本的课程内稳定身份；`PublishedKnowledgeGraphNode` 是具体版本节点，二者不得混用。
- 题目绑定保存来源图谱版本和节点；发布作业时将绑定修订冻结到 `AssignmentQuestionConceptSnapshot`。
- 只有发布快照成功解析到正式图谱的已批改答案才能形成有效 `StudentAnswerConceptEvidence`。
- 证据与 `StudentCourseConceptMasteryRevision` 采用追加式修订，不覆盖历史；相同输入指纹应幂等复用。
- 不自动回填该能力上线前已发布作业；如未来需要回填，必须单独设计可审查迁移、范围确认和回滚方案。

## 19. 完成任务时的沟通

最终回复应简洁、可核验，并优先说明结果：

- 完成了什么。
- 主要修改文件。
- 数据库或配置是否变化。
- 实际运行的测试及结果。
- 未运行的检查及原因。
- 仍存在的限制、风险或下一步。

不要宣称未实际运行的测试已通过，不要用“应该可以”代替验证结果。
